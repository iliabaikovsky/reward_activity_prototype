/** Прототипные флаги UI — один источник правды для временно скрытых элементов. */

/** Скрыть badge на строках Upcoming (элемент остаётся в DOM). */
export const HIDE_TRANSACTION_BADGES = true

/** Скрыть сумму за день в шапке группы Activity feed. */
export const HIDE_DAY_SUMMARY = true
